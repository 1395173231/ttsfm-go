# 开发版本 Dockerfile - 包含热重载
FROM golang:1.25-alpine

# 安装开发工具
RUN apk add --no-cache git ca-certificates tzdata curl

# 安装 air 用于热重载（使用新的仓库地址）
RUN go install github.com/air-verse/air@latest

# 设置工作目录
WORKDIR /app

# 复制 go.mod 和 go.sum
COPY go.mod go.sum ./

# 下载依赖
RUN go mod download

# 暴露端口
EXPOSE 8080

# 使用 air 启动（热重载）
CMD ["air", "-c", ".air.toml"]
